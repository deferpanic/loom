aspect {
  imports (
    "github.com/deferpanic/vizcli"
  )
  pointcut: call(strconv.Itoa(int i))
  advice: {
    before: {
      myCnt += 1
      vizcli.PostVal(myCnt)
    }
  }
}

aspect {
  pointcut: execute(*)
  advice: {
    before: {
      var myCnt int =0
    }
  }
}
